name: example
description: Example for how to use an ImplicitlyAnimatedReorderableList
publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_slidable: ^3.1.0
  implicitly_animated_reorderable_list_2:
    path: ../../implicitly_animated_reorderable_list_2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
